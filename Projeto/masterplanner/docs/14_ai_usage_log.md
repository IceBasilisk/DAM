# 14 — AI Usage Log (ongoing)

> Update **as you use AI**, not at the end. It is the proof of *how* the work evolved.
> The full prompt goes to `prompts/`; here goes the summary and the critical decision.
> The `AI-Assisted:` *trailer* in commits links to this log.

## How to fill in

One line per relevant interaction (non-trivial ones). Be honest about what you **rejected**.

| Date | Tool | Goal | Prompt (ref. in `prompts/`) | Result | Accepted / Rejected / Edited | Why |
|---|---|---|---|---|---|---|
| 2026-05-11 | Android Studio AI | Refactor Login/Signup to Compose | N/A | Converted activities, added Firebase logic | Accepted | Integrated existing screens with Firebase auth |
| 2026-05-11 | Android Studio AI | Setup Compose Environment | N/A | Updated gradle, added dependencies/plugins | Accepted | Required for Compose 1.7+ and Kotlin 2.0 |
| 2026-05-11 | Android Studio AI | Define Data Models & Navigation | N/A | `Task`, `Roadmap` classes + `Screen` enum nav | Accepted | Architecture foundation |
| 2026-05-12 | Android Studio AI | Add Logout to Main Menu | N/A | Dropdown menu in top bar | Accepted | UI/UX requirement |
| 2026-05-17 | Android Studio AI | Unified Bottom Bar | N/A | `MasterPlannerBottomBar` in `CommonUI.kt` | Accepted | Shared navigation across 4 screens |
| 2026-05-24 | Android Studio AI | Fix Library/Creation bugs | N/A | Callback fixes and ID handling | Accepted | Stability fixes |
| 2026-05-24 | ChatGPT (GPT-5.5) | Diagnose Firestore save failure                   | Not yet archived in prompts/ | Analyzed ViewModel save methods and identified likely causes (rules, auth, timestamps)                   | Accepted                     | Guided debugging process before Logcat investigation                  |
| 2026-05-24 | ChatGPT (GPT-5.5) | Debug Firestore permission error                  | Not yet archived in prompts/ | Interpreted PERMISSION_DENIED logs and identified Firestore Security Rules as root cause                 | Accepted                     | Resolved inability to save roadmaps/tasks to Firestore                |
| 2026-05-24 | ChatGPT (GPT-5.5) | Improve timestamp handling in Firestore listeners | Not yet archived in prompts/ | Added defensive sorting using timestamp null fallback (`timestamp?.seconds ?: 0L`)                       | Accepted                     | Prevented issues while waiting for `@ServerTimestamp` synchronization |
| 2026-06-02 | ChatGPT (GPT-5.5) | Design roadmap/task deletion workflow             | Not yet archived in prompts/ | Proposed confirmation dialog with optional cascading task deletion and Firestore batch deletion approach | Accepted                     | Matches project data-management requirements                          |
| 2026-06-02 | Android Studio AI | Custom Wheel Picker for Duration | N/A | `AndroidView` wrapping `NumberPicker` | Edited | Initial syntax error on `Modifier.padding` was fixed manually |
| 2026-06-02 | ChatGPT (GPT-5.5) | Plan Task Library editing and filtering features  | Not yet archived in prompts/ | Proposed BootyBag-style filtering and edit/delete actions via pencil menu                                | Accepted                     | Reuses established UX patterns in project                             |
| 2026-06-02 | Claude (Sonnet 4.6) | Identify performance bottlenecks in Compose app | Not yet archived in prompts/ | 5 issues found: DottedBackground redraws, duplicate Firestore listeners, colorResource on recompose, currentItems rebuild, eager saveRoadmapItems | Accepted | Structural fixes with clear reasoning per issue |
| 2026-06-02 | Claude (Sonnet 4.6) | Fix duplicate Firestore task library listeners | Not yet archived in prompts/ | Move `listenToTaskLibrary()` to ViewModel `init` block, remove LaunchedEffect calls from screens | Accepted | Cleaner and correct; init runs once automatically |
| 2026-06-02 | Claude (Sonnet 4.6) | Replace colorResource calls with CompositionLocal | Not yet archived in prompts/ | AppColors data class + LocalAppColors + single resolution in MasterPlannerTheme | Accepted | Eliminates resource lookups on every recomposition |
| 2026-06-03 | Claude (Sonnet 4.6) | Fix dead search bar in TaskLibraryScreen | Not yet archived in prompts/ | Wire searchQuery state + remember(libraryTasks, searchQuery) filter + pass filteredTasks to list | Accepted | Consistent with pattern already used in BootyBagDialog |
| 2026-06-03 | Claude (Sonnet 4.6) | Fix input text color in OutlinedTextField | Not yet archived in prompts/ | Add focusedTextColor and unfocusedTextColor to colors block | Accepted | Previously falling back to MaterialTheme default |
| 2026-06-03 | Claude (Sonnet 4.6) | Fix 4 UI bugs (splash centering, search icon, toggle border overflow, library delete) | Not yet archived in prompts/ | 4 targeted fixes across SplashScreen, MainMenuScreen, RoadMapEditorScreen, TaskLibraryScreen | Edited | Toggle border fix required understanding clip-before-border ordering in Compose; delete fix required tracing Firestore snapshot listener chain |
| 2026-06-03 | ChatGPT (GPT-5.5) | Implement wheel-style duration picker             | Not yet archived in prompts/ | Recommended Android `NumberPicker` wrapped with `AndroidView` in Compose                                 | Accepted                     | Closest behavior to native Android timer interfaces                   |
| 2026-06-03 | Android Studio AI | Task Library Search & Delete | N/A | Added `searchQuery` filtering and Delete option | Accepted | UI/UX improvement and CRUD functionality |
| 2026-06-03 | Android Studio AI | Roadmap Deletion with tasks popup | N/A | `AlertDialog` asking to delete sub-tasks | Accepted | Data management requirement |
| 2026-06-03 | ChatGPT (GPT-5.5) | Debug RoadMapEditorScreen compile errors             | Not yet archived in prompts/ | Identified duplicate RoadmapItem declaration/import conflict and incorrect ReorderableItem usage                                                                 | Accepted                     | Helped locate root causes behind unresolved reference errors                                  |
| 2026-06-03 | ChatGPT (GPT-5.5) | Analyze unresolved reference cascade in Compose file | Not yet archived in prompts/ | Explained that parser failures (incorrect ReorderableItem call, undefined task variable, brace mismatches) can cause subsequent composables to appear unresolved | Accepted                     | Used to narrow debugging effort to first compiler error rather than reported secondary errors |
| 2026-06-04 | ChatGPT (GPT-5.5) | Diagnose Compose compilation errors in RoadMapEditorScreen | Not yet archived in prompts/ | Identified scope issue with nested sealed class `RoadmapItem` and generic type inference problem in `remember {}` block | Accepted                     | Explanation matched compiler errors and clarified Kotlin scoping rules |
| 2026-06-04 | ChatGPT (GPT-5.5) | Refactor RoadmapItem model structure                       | Not yet archived in prompts/ | Recommended moving `RoadmapItem` to top-level declaration and removing unnecessary casts                                | Accepted                     | Improved code organization and resolved unresolved reference issue     |
| 2026-06-04 | ChatGPT (GPT-5.5) | Fix generic type inference for `toMutableStateList()`      | Not yet archived in prompts/ | Suggested explicit `map<RoadmapItem>` typing and elimination of unsafe casts                                            | Accepted                     | Resolved "Cannot infer type for type parameter T" compiler error       |
| 2026-06-06 | Claude (Sonnet 4.6) | Fix back navigation from CreateTask to TaskLibrary | Not yet archived in prompts/ | Add previousScreen variable in MainActivity; set it before navigating to CreateTask | Accepted | Minimal change, no screen files modified |
| 2026-06-06 | Claude (Sonnet 4.6) | Fix toggle border bleeding outside container | Not yet archived in prompts/ | Move .clip() before .border() on Row; remove individual borders from tab Box composables | Accepted | Root cause was draw order: clip must precede border to mask children |
| 2026-06-06 | Claude (Sonnet 4.6) | Cascade delete from Task Library to all Roadmaps | Not yet archived in prompts/ | Rewrite deleteLibraryTask to batch-update all roadmap itemEntries after deleting from task_library | Accepted | Required Firestore batch write across roadmaps collection |
| 2026-06-06 | Claude (Sonnet 4.6) | Debug non-working AI name suggestion button | Not yet archived in prompts/ | Three bugs found: button hidden when roadmapId=="library", race condition on roadmapTitle, wrong Gemini API version path (v1 → v1beta) | Accepted | v1beta path was the likely silent failure cause |
| 2026-06-06 | Claude (Sonnet 4.6) | Add free/premium toggle with limits | Not yet archived in prompts/ | PremiumManager singleton with SharedPreferences persistence, toggle in FreemiumScreen, crown icon in bottom bar, limits wired into MainMenu and RoadMapEditor | Accepted | Clean separation via object; limits readable reactively via StateFlow |
| 2026-06-09 | Claude (claude.ai) | Guide on implementing Roadmap and Task Firebase logic using NotesProXMLViews3 as reference pattern | Not archived | Full guide mapping Notes app pattern (Utility, ViewModel, CRUD) to MasterPlanner Firestore nested collection structure; included Roadmap.kt, Task.kt, Utility.kt, RoadmapViewModel.kt rewrites and screen wiring | Accepted | Established core Firestore architecture: `users/{uid}/roadmaps/{id}/tasks/{id}` and the `saveRoadmap`/`saveTask` CRUD pattern |
| 2026-06-09 | Claude (claude.ai) | Fix `onNavigate` type mismatch `(Screen)` vs `(Screen, String?)` and `items()` Int inference error in MainMenuScreen | Not archived | Unified navigate lambda `(Screen, String?) -> Unit`; wrapper `{ screen -> onNavigate(screen, null) }` for screens without roadmapId; added `import androidx.compose.foundation.lazy.items` | Accepted | Compile errors blocking build |
| 2026-06-09 | Claude (claude.ai) | Fix `Color(task.colorHex)` String→Color and `Icon(task.iconName)` String→ImageVector type errors in TaskLibraryScreen | Not archived | `android.graphics.Color.parseColor()` for hex string conversion; `iconFromName()` mapping function String→ImageVector added to Utility.kt | Accepted | Type mismatches preventing compilation |
| 2026-06-09 | Claude (claude.ai) | Fix multiple `onNavigate` errors in MainActivity — missing `roadmapViewModel`, `roadmapId`, and wrong parameter types across screen calls | Not archived | Added missing `roadmapViewModel` to `TaskLibraryScreen` and `CreateTaskScreen` calls; added `roadmapId = currentRoadmapId` to `CreateTaskScreen`; updated `onBack` to navigate to `RoadMapEditor` not `MainMenu` | Accepted | Several unresolved reference and argument mismatch errors |
| 2026-06-09 | Claude (claude.ai) | Debug "Failed to create roadmap" and task save failures | Not archived | Root cause: `Timestamp.now()` as default field value breaks Firestore reflection-based deserializer; fix: `@ServerTimestamp val timestamp: Timestamp? = null` on both models; also switched all CRUD from `addOnCompleteListener` to `addOnSuccessListener`/`addOnFailureListener` to guarantee main-thread callbacks | Accepted | Core Firestore save was silently failing on both Roadmap and Task creation |
| 2026-06-09 | Claude (claude.ai) | Ensure CreateRoadmapDialog closes after successful save | Not archived | Confirmed `showDialog = false` inside `addOnSuccessListener` is correct; actual fix was the `@ServerTimestamp` model change and listener switch above which unblocked the callback reaching the UI | Accepted | Dialog was not dismissing after save despite correct logic |
| 2026-06-09 | Claude (claude.ai) | Fix `RoadmapItem.add()` type mismatch — list inferred as `MutableList<RoadmapItem.Task>` instead of `MutableList<RoadmapItem>` | Not archived | Cast each element with `as RoadmapItem` in the `map {}` call to force Kotlin to infer `SnapshotStateList<RoadmapItem>` | Accepted | Duration items could not be added to `currentItems` due to wrong inferred generic type |
| 2026-06-09 | Claude (claude.ai) | Obtain hex color string from `R.color` resource IDs for Task creation | Not archived | Two options provided: hardcode hex strings matching `colors.xml` values (simpler); or `ContextCompat.getColor()` + `String.format("#%06X", 0xFFFFFF and colorInt)` at runtime | Accepted (hardcoded) | Hardcoded hex list chosen as simpler and sufficient for fixed palette |
| 2026-06-09 | Claude (claude.ai) | Implement real task search/filter in BootyBagDialog using actual Firestore library tasks | Not archived | Replaced hardcoded `bootyTasks` list with `libraryTasks` StateFlow from ViewModel; added `searchQuery` state and `filteredTasks` derived value; updated `onImportSelected` to pass full `RoadmapItem.Task` with `iconName`/`colorHex`; empty state messages added | Accepted | BootyBag was showing hardcoded dummy data instead of real library tasks |
| 2026-06-09 | Claude (claude.ai) | Show real saved icon and color on EditorTaskCard | Not archived | Added `iconName`/`colorHex` params to `EditorTaskCard` and `RoadmapItem.Task`; replaced hardcoded `when(title.contains(...))` icon guessing with `Utility.iconFromName(iconName)`; `Color(colorHex.toColorInt())` for background | Accepted | Cards were showing wrong icons and ignoring saved colors |
| 2026-06-10 | ChatGPT (GPT-5.5) | Center Splash Screen title                         | Not yet archived in prompts/ | Suggested replacing title Text modifier with fillMaxWidth() and TextAlign.Center                                         | Accepted                     | Fixed alignment of MASTER PLANNER title on splash screen                              |
| 2026-06-10 | ChatGPT (GPT-5.5) | Remove Main Menu search icon                       | Not yet archived in prompts/ | Identified Icon block to remove while preserving top bar structure                                                       | Accepted                     | UI requirement to simplify top bar                                                    |
| 2026-06-10 | Claude (claude.ai) | Display real roadmap title in RoadMapEditorScreen header and persist task order + duration badges | Not archived | Added `_currentRoadmap` StateFlow with second `addSnapshotListener` on roadmap document in `listenToTasks()`; created `RoadmapItemEntry.kt` data class; added `saveRoadmapItems()` batch update to ViewModel; `currentItems` loads from `itemEntries` on screen open, falls back to timestamp-sorted tasks | Accepted | Title was hardcoded string resource; item order and durations were lost on navigation |
| 2026-06-10 | Claude (claude.ai) | Create dedicated RoadMapEditorTopBar with save-on-back and roadmap title badge | Not archived | New `RoadMapEditorTopBar` composable: back `IconButton` calling `saveRoadmapItems { onBack() }`, centered gold title `Surface` badge with `TextOverflow.Ellipsis`, balancing `Spacer`; replaced generic `MasterPlannerTopBar` | Accepted | Generic top bar lacked save-on-back behavior and couldn't display the dynamic roadmap title |
| 2026-06-10 | Claude (claude.ai) | Make duration badges deletable by tapping; make tasks created in roadmap also save to library; add Oracle failure message | Not archived | `TimerBadge`: added `onDelete` lambda + `Modifier.clickable` + close icon. `CreateTaskScreen`: call both `saveTask()` and `saveTaskToLibrary()` when `roadmapId != "library"`. AI button: added `hasSuggestionBeenAttempted` StateFlow; "Oracle" failure message; button hidden outside roadmap context | Accepted | Three separate UX gaps addressed in one session |
| 2026-06-10 | Claude (claude.ai) | Integrate Gemini API using AISimpleCalls sample as reference instead of ClaudeService | Not archived | `GeminiService.kt` using OkHttp, `gemini-2.5-flash-lite` model, `generateContent` endpoint, JSON parsed with `org.json.JSONArray`; added `fetchTaskSuggestions()`, `_isSuggesting`, `_suggestedTaskNames`, `_hasSuggestionBeenAttempted` to ViewModel; `implementation("com.squareup.okhttp3:okhttp:4.12.0")` added to gradle | Accepted | Replaced `HttpURLConnection` approach with OkHttp pattern matching provided AISimpleCalls sample |
| 2026-06-11 | Claude (claude.ai) | Add looping background music starting from Splash screen | Not archived | `MusicManager.kt` singleton: `MediaPlayer.create(context, R.raw.background_music)`, `isLooping = true`, `setVolume(0.4f, 0.4f)`; `start()` in `MainActivity.onCreate()`; `pause()`/`resume()` in `onPause`/`onResume`; `release()` in `onDestroy()` | Accepted | Multimedia mandatory requirement |
| 2026-06-11 | Claude (claude.ai) | Add Freemium paywall screen blocking 4th roadmap or 7th task | Not archived | `FreemiumScreen.kt` with gradient background, `FreemiumFeatureRow` helper, free vs Captain's Edition comparison, simulated $4.99/month CTA button, dismiss link; `Screen.Freemium` added to enum; gate: `roadmaps.size >= 3` in MainMenu, `count { Task } >= 6` in RoadMapEditor FAB and import | Accepted | Freemium mandatory requirement |
| 2026-06-11 | Claude (claude.ai) | Implement offline mode infrastructure | Not archived | Firestore `PersistentCacheSettings` (50 MB) at app startup; `NetworkMonitor.kt` with `ConnectivityManager.NetworkCallback` emitting `Flow<Boolean>` via `callbackFlow`; `_isOnline` StateFlow + `startMonitoringNetwork()` in ViewModel; `OfflineBanner` animated composable; AI button disabled with "Oracle unreachable offline"; `CloudOff` icon in `RoadMapEditorTopBar`; `ACCESS_NETWORK_STATE` permission | Accepted | Offline mandatory requirement |
| 2026-06-13 | ChatGPT (GPT-5.5) | Diagnose Task Library delete issue                 | Not yet archived in prompts/ | Reviewed deletion flow and identified likely Firestore document ID mismatch; suggested snapshot listener fix and logging | Edited                       | Analysis was used for debugging but root cause required project-specific verification |
| 2026-06-13 | ChatGPT (GPT-5.5) | Fix Add Duration segmented control border overflow | Not yet archived in prompts/ | Explained border overflow cause and recommended removing inner selected-tab border                                       | Accepted                     | Resolved visual issue where selected tab border exceeded parent container             |
| 2026-06-13 | Claude (claude.ai) | Fix large empty gap below top bar in MainMenuScreen | Not archived | Identified double `innerPadding` application: on outer `Column` and again on `LazyColumn`; removed `.padding(innerPadding)` from `LazyColumn` keeping only horizontal padding; reduced `MasterPlannerTopBar` vertical padding from `padding(16.dp)` to `padding(horizontal=16.dp, vertical=8.dp)` | Accepted | Visual layout bug confirmed in screenshot |
| 2026-06-13 | Claude (claude.ai) | Implement drag-and-drop reordering for roadmaps in MainMenuScreen | Not archived | Added `sh.calvin.reorderable:reorderable:2.4.3`; `mutableStateListOf<Roadmap>()` local list; `LaunchedEffect(roadmaps)` sync guard; `rememberReorderableLazyListState` + `LazyColumn(state=)`; `ReorderableItem` per card; `DragHandle` icon with `Modifier.draggableHandle()`; `reorderRoadmaps()` batch Firestore update on `isAnyItemDragging → false`; `sortOrder: Int` field on `Roadmap` | Accepted | Drag-and-drop reorder requirement; also required correcting hallucinated `ReorderableLazyColumn` (see errors table) |
| 2026-06-14 | Claude (claude.ai) | Implement drag-and-drop for tasks and duration badges in RoadMapEditorScreen | Not archived | Same `sh.calvin.reorderable` pattern; `items()` with stable keys replacing `itemsIndexed()`; `ReorderableItem` for both `RoadmapItem.Task` and `RoadmapItem.Duration`; drag handle on `EditorTaskCard` (`isDragging`/`dragHandleModifier` params) and `TimerBadge`; `saveRoadmapItems()` on drag end | Accepted | Drag-and-drop for editor items; required separate clarification exchange on correct API usage |
| 2026-06-14 | Claude (claude.ai) | Implement drag-and-drop for library tasks in TaskLibraryScreen | Not archived | Same pattern; `taskList` local mutable list synced via `LaunchedEffect`; `onMove` index offset of 3 (spacer + search bar + header items); drag disabled when `searchQuery.isNotBlank()`; `reorderLibraryTasks()` batch Firestore update on drag end; `sortOrder: Int` on `Task`; `LibraryTaskCard` updated with `isDragging`/`dragHandleModifier` params | Accepted | Drag-and-drop for library; disabled during search to prevent index errors |

| 2026-06-19 | ChatGPT (GPT-5.5 Thinking) | Enforce free-tier total task limit on task creation entry points | `prompts/architecture_prompts.md` #p33; `prompts/compose_prompts.md` #p26; `prompts/debugging_prompts.md` #p19; `prompts/refactoring_prompts.md` #p18 | `MainActivity`: observes `libraryTasks` and `PremiumManager.isPremium`; blocks Task Library and Roadmap Editor create-task callbacks at `Screen.Freemium` when the free total task cap is reached. `CreateTaskScreen`: replaces roadmap-local save guard with global library-count guard. `FreemiumScreen`: copy changed to 6 total tasks. | Accepted | Fixes bypass where the 7th total task could still enter Forge New Task from Task Library or Booty Bag create link |


## AI errors / hallucinations detected

| Date | What the AI got wrong | How it was noticed | Fix |
|---|---|---|---|
| 2026-05-17 | Missed Compose Compiler plugin for Kotlin 2.0 | Build failed with specific error message | Added `kotlin-compose` plugin to gradle files |
| 2026-05-24 | Used invalid `Modifier.padding` overload | Compiler error (no horizontal/top combination) | Changed to `start/end/top` or `horizontal/vertical` |
| 2026-06-02 | Attempted to delete files using `rm` on Windows | Shell command failed | Used `del` command instead |
| 2026-06-04 | Repeated invalid `Modifier.padding` overload | Compiler error (no horizontal/top combination) | Fixed padding parameters in `AddDurationDialog` |
| 2026-06-04 | Initially suspected timestamp handling or save implementation issues before Logcat evidence was available | Firestore logs later showed explicit `PERMISSION_DENIED` error | Investigated Logcat output and corrected diagnosis to Firestore Security Rules |
| 2026-06-04 | Generated ReorderableItem usage referencing undefined variable task.id              | Compiler reported "Unresolved reference: task"               | Replaced generated key with valid item key and verified API usage against installed reorderable library version |
| 2026-06-06 | Mixed two different RoadmapItem definitions (imported model and local sealed class) | Compiler reported type conflicts and inconsistent references | Kept a single RoadmapItem definition and removed the duplicate declaration/import                               |
| 2026-06-06 | Assumed Task Library delete issue was caused by Firestore document ID mismatch | Testing showed delete problem originated elsewhere in application logic | Applied project-specific fix after inspecting actual implementation |
| 2026-06-06 | Suggested `remember { }` for color resolution as interim fix, then immediately corrected it as wrong approach | Self-corrected mid-response | Replaced with CompositionLocal recommendation |
| 2026-06-09 | Initial delete debug assumed filtering bug caused reappearance; actual issue was cascade not implemented | Code inspection showed deleteLibraryTask only touched task_library collection | Rewrote to batch-update all roadmaps |
| 2026-05-09 | Missed Compose Compiler plugin for Kotlin 2.0 | Build failed with specific error message | Added kotlin-compose plugin to gradle files |
| 2026-05-09 | Used invalid Modifier.padding overload | Compiler error (no horizontal/top combination) | Changed to start/end/top or horizontal/vertical |
| 2026-06-10 | Attempted to delete files using `rm` on Windows | Shell command failed | Used `del` command instead |
| 2026-06-11 | Repeated invalid Modifier.padding overload | Compiler error (no horizontal/top combination) | Fixed padding parameters in AddDurationDialog |
| 2026-06-11 | Suggested `ReorderableLazyColumn` wrapper composable as the drag-and-drop API | User checked the `sh.calvin.reorderable` GitHub repo and confirmed the class no longer exists in the current API | Replaced with plain `LazyColumn(state = lazyListState)` + `ReorderableItem` per item — the correct current API |
| 2026-06-13 | Used `import sh.calvin.reorderable.reorderableItem` (lowercase function) | Compiler error — correct symbol is `ReorderableItem` (uppercase composable) | Corrected import and all call sites to `ReorderableItem` |
| 2026-06-13 | Proposed `ClaudeService.kt` using `HttpURLConnection` for the Gemini API call | Inconsistency with the OkHttp-based `AIAssistantGemini` pattern in the provided AISimpleCalls sample | Replaced entirely with `GeminiService.kt` using OkHttp, matching the sample's pattern |

> The final reflection (what went well/badly overall) goes to `docs/15_postmortem.md`.
